<script lang="ts">
  import Listing from './Listing.svelte';
  let { data } = $props();
</script>

<!-- Ключ на город: переход между городами переиспользовал бы тот же компонент,
     и догруженные «показать ещё» карточки прошлого города остались бы в
     состоянии. Пересоздание дешевле, чем сброс состояния эффектом. -->
{#key data.city.slug}
  <Listing {...data} />
{/key}
