<script lang="ts">
  import Listing from './Listing.svelte';
  let { data } = $props();
</script>

<!-- Ключ на город И полосу: переход между городами переиспользовал бы тот же
     компонент, и догруженные «показать ещё» карточки прошлого города остались
     бы в состоянии. Полоса здесь по той же причине — она меняется параметром
     адреса, компонент при этом не пересоздаётся, и хвост «по дате и времени»
     доехал бы до полосы «идёт сейчас». Пересоздание дешевле, чем сброс
     состояния эффектом. -->
{#key `${data.city.slug}?${data.kind ?? ''}`}
  <Listing {...data} />
{/key}
