-- 007_tg_event_covers.sql — обложки витрины tg-событий.
--
-- Решение фаундера 23.08 (отменяет «чужие медиа не показываем» из 006):
-- превью поста идёт на витрину — ссылка на первоисточник стоит на каждой
-- карточке, модель та же, что у поисковиков с картинками.
--
-- Байты храним у себя, а не хотлинким: превью-CDN телеги протухает за дни
-- (замер 23.08: все ссылки cdn4.telesco.pe из съёма 21.08 уже отдавали 404).
-- Импортёр перечитывает страницу поста в момент заливки и качает свежие
-- байты. В базе, а не на диске — обложки переезжают в изолированную БД
-- вместе с таблицей, одним DATABASE_URL.

ALTER TABLE afisha_tg_events
    ADD COLUMN IF NOT EXISTS cover      BYTEA,
    ADD COLUMN IF NOT EXISTS cover_mime TEXT;
